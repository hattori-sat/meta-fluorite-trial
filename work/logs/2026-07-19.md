# Working log — 2026-07-19

時刻は特記がない限りAsia/Tokyo。これは完成手順書ではなく、観測を失わないための逐次log。

## Read-only baseline investigation

- Mac workspaceはcommitなし、originなしと確認。
- Git対象外のlocal設定を使ってbuild hostへの接続を確認。
- `$AGL_ROOT`のfixed repo manifest、build conf、layer、cache、artifactを調査。
- Raspberry PiとQEMU x86-64の完成manifestにFluorite、Flutter Engine 3.38.3、Vulkan packageを確認。
- 長時間build、sync、clean、既存AGL treeの編集は行わなかった。

## Clone confirmation

- Macとbuild hostの`$HOME/work/meta-fluorite-trial`を確認。
- origin、main、HEAD `25a2b328e03a98bbf76833d077756f370aea7d7c`が一致し、両方clean。
- `$HOME/work/fluorite` rootはGit repositoryではなく、過去成果物・source・資料の参照directory。

## Mac QEMU observations

- hostはApple Silicon arm64、macOS 26.5.1。
- QEMU 11.0.1。aarch64 binaryはHVF/TCG、Cocoa display、virtio-gpu-pciを提供。
- 既存記録ではqemux86-64 + TCG + llvmpipeでFluorite scene初期化後に`libLLVM.so.18.1` SIGSEGV。

## Work-management decision

- WIPを原則1件に制限。
- FLR-0001をactive ticketに設定。
- raw execution、durable context、decision、evidence、ticketを別fileへ分離。
- PDCA checker agentをread-onlyで追加。

## Next exact action

`meta-local`と完全なfixed manifestをcanonical cloneへ取得する。

## Canonical management-tree sync

- `rsync -ani --exclude='.git/'`で変更対象を事前確認。
- `--delete`を使わず、現在workspaceからMac側canonical cloneへ同期。
- cloneの`.git`と`LICENSE`は保持。
- commit、stage、pushは実施していない。
- PDCA記録2件の追記同期時、destinationを`work/`directoryとして指定したため重複fileを誤配置した。
- 誤配置を確認後、対象2件だけを`/tmp/meta-fluorite-trial-misplaced-*.md`へ退避し、`work/tickets/`と`work/logs/`の正しいpathへ再同期した。退避fileは回収可能。

## Privacy correction

- repository文書に接続先情報、個人account、個人名を含む絶対pathが残っていることを検出。
- 値を再掲せず、`$BUILD_HOST`、`$AGL_ROOT`、`$HOME`、Git remote `origin`などのrole表現へ置換。
- 今後はprivacy check失敗をticket進行のblockerとする。
- `scripts/check-repository-privacy.sh`、指定表現scan、Markdown内部link、TOML構文を実行しPASS。

## Problem-solving sequence correction

- 運用順序を、目的、成功指標、4W1H（Whyを除く）による層別、重点選定、process解析、問題点特定、真因分析、最小対策、PDCAへ変更。
- 4W1HのWhoは個人名ではなくrole/component責務として記録する。
- active ticket FLR-0001を新しい形式へ移行し、problem pointとroot-cause evidenceを分離した。

## Initial Fluorite/rendering source investigation

- `$LEGACY_ROOT`のFlutter demo、Dart plugin、ivi launcher、Flutter recipe、custom layer、Filament recipeをread-onlyで調査。
- demo app、scene群、native readiness、Dart-to-native plugin境界を確認。
- launcherにFlutter Embedder APIとWayland EGL/Vulkan backendが存在することを確認。
- Yocto customizationがnative Filament plugin、Filament library、Vulkan loaderを結ぶことを確認。
- Filament recipeがVulkan/Wayland featureを持ち、customizationでversion/build/install調整されることを確認。
- composition方式、thread ownership、surface/texture ownershipはUNKNOWNとして分離。
- AGL、demo、engine/embedder、Filament bridge、Vulkan/GPU、source MCPを個別ticket化。
- build hostへの追加CLIは保留。Mac上のstdio MCP + fixed SSH queryを初期案とした。

## Canonical repository and branch workflow correction

- 現在のCodex workspaceがoriginを持たない別Git repositoryであることを再確認。
- canonical cloneは`origin/main`を追跡するrepositoryとして確認。
- 今後はcanonical guardに失敗したrepositoryでmutating作業を止める方針を追加。
- branch flowを`main → dev-<milestone> → feature-<ticket>-<slug>`に決定。
- GitHub pull requestのbranch方向を検査するworkflowを追加。
- current ticket用branchは`dev-foundation`から`feature-flr-0001-repository-baseline`を作る計画。
- 最初のguard実行は別workspaceをcurrent worktreeとして検出し、branch作成前に正しく停止した。
- canonical worktreeへ移動してguardとprivacy checkがPASSした後、`dev-foundation`を`main`から作成。
- `feature-flr-0001-repository-baseline`を`dev-foundation`から作成し、未commit変更をfeature branch上に保持。
- commit、push、pull requestは実施していない。

## First repository checkpoint

- `feature-flr-0001-repository-baseline`上で管理tree 40 filesをcommit `160bac0`へ固定した。
- linked worktreeを使い、canonical clone以外のworkspaceへ変更が入らないよう分離した。
- canonical main worktreeは`dev-foundation`へ戻し、feature作業だけをlinked worktreeへ保持した。

## Baseline artifact import

### Facts

- build hostのAGL checkoutを変更せず、repo manifest commandをsource rootから実行して35 projectのfixed manifestを保存した。
- allowlistを`meta-local`、Raspberry Pi/QEMU conf、fixed manifest、`meta-agl/scripts/aglsetup.sh`のlocal diffに限定した。
- `meta-local`は45 files、`LAYERSERIES_COMPAT`は`scarthgap`、layer priorityは90。
- confのhost固有source/build pathを`@AGL_ROOT@`と`@AGL_BUILD_DIR@`へ置換した。
- patch metadataのauthor/trailer identityをrole表現へ置換した。独立監査で初期privacy scanが`layers/`を除外していた問題を検出したため、全project layerを検査対象へ変更し、email形式の値が残らないことをtest化した。patch hunkは変更せず、identity-normalized tree hashでcontent同一性を固定した。
- このticketではlive build directoryへconf/layerを適用せず、imported patchのfunctional hunkも変更していない。そのため`bitbake -e`/parse gateは起動せず、mini PCをpush後の同一commitへ揃えたExecution acceptanceまで実効metadataをUNKNOWNとして残した。
- legacy参照directoryから先に同期された旧Filament patch 3件はcurrent build-host treeに存在しないとdry-runで判定し、作業treeから削除した。元fileはlegacy参照directoryから回収可能。
- fixed manifest、sanitized conf、source/sanitized layer treeのhashとfile countを`manifests/baseline-sources.lock`へ記録した。
- demo patch 3件、shell環境file 1件、commented-out mipmap patch 1件は対応recipe/appendのactive `SRC_URI`から参照されていない。採取baselineのbyte integrityを優先して保持し、機能済みcountermeasureとは扱わない。実効metadata確認と整理判断はFLR-0008へ分離した。

### Inference

fixed manifest、外部layer lock、current `meta-local`、build別confが単一repositoryへ揃い、以後のQEMU/Raspberry Pi差分を同じ入力identityへ結べる。

### Hypotheses retained

1. current `meta-local`は既存imageを再現する最小のcustomization集合である。反証は、実効BitBake metadataまたはimage manifestに未取得のlocal変更が現れること。
2. conf snapshotには試行途中の重複・不要設定が残る可能性がある。反証/確認にはfixed environmentでのparseと実効variable evidenceが必要。

## DDD and agent/MCP development environment

### Facts

- AGL、Yocto、Fluorite demo、Flutter runtime、Filament、Graphics、Target Validation、Executionを8 bounded contextsへ分離した。
- AGLとYoctoは別の用語、owner agent、MCP server、handoff contractを持つ。
- 10 custom agentをproject scopeへ配置し、investigatorは担当MCP 1個だけをenableする設定にした。PDCA checkerはdomain MCPを持たない。
- 8 stdio MCPをdependency-free Pythonで実装し、transport/pagination/redaction/evidence envelopeだけをshared kernelにした。
- command runnerはregistered runbook IDだけを受け付け、任意shellを受け付けない。state-changing executionはtool approvalに加えてticket上の承認を要求する。
- `make setup`はcheck-only、`make verify`はlocal/CI共通gateとした。sanitized confのmaterializerはdry-run defaultかつ既存confの上書きを拒否する。

### Inference

専門agentへ必要なdomain vocabularyとevidenceだけを渡し、primary contextへraw source/logを流さない開発基盤ができた。

### UNKNOWN

- fresh Codex taskでcustom agentがproject MCP overrideを実際に読み込むことは、configuration reload後のsmoke testまでUNKNOWN。
- MCPはrepository baselineを観測できるが、build host/targetのlive evidence contractは今後のticketで接続・検証が必要。
- Mac QEMUの3D描画、Raspberry Pi build/bootはこのfoundation作業では実行していない。

## Official AGL workflow alignment and host split

### Facts

- official Automotive Grade Linux `trout` documentationで、native Linux build host、AGL manifest repositoryからの`repo init`/`repo sync`、`aglsetup.sh`によるbuild conf生成、BitBakeという順序を確認した。
- official host guidanceはnative Linuxと100 GB以上の空き容量を推奨する。Yocto buildをMacへ移す根拠はない。
- build責務をLinux mini PC、QEMU実行検証責務をMacへ固定した。
- captured `aglsetup.manifest`をRaspberry Pi/QEMUの両buildから取得し、個人/host情報をrole placeholderへ置換してhashを記録した。
- 両buildのresolved AGL featuresは`agl-all-features`、`agl-app-framework`、`agl-demo`、`agl-flutter`、`agl-kuksa-val`、`agl-netboot`、`agl-pipewire`、`agl-selinux`。
- Raspberry Piのsetup inputは`raspberrypi4`で、fixed AGL machine templateが実効`MACHINE = "raspberrypi4-64"`へ解決することをsourceで確認した。
- official Linux QEMU手順は`runqemu`/KVM中心であり、Apple Silicon macOSのgraphics起動contractはproject側で別に検証する必要がある。
- mini PCはUbuntu 22.04.5、RAM 28,855 MiB、Yocto cache/tmp filesystem空き141 GiB、source filesystem空き16 GiB、checked host tools/locale PASS、system Python 3.10.12。

### Inference

source/config/buildはmini PC、artifact identity/QEMU/scene acceptanceはMacというhandoffを明示しないと、official Linux QEMU結果とMac固有TCG/HVF結果を誤って同一視する。

Yocto build要件として追加packageは現時点で検出されなかった。Codex CLIはmini PCに不要であり、remote MCPだけがPython 3.10 runtime smokeを通せるかを別に確認する。

`setup-build-host.sh`をmini PCへstreamしてcheck-only実行した。初回はproject検索用`rg`だけがMISSINGだったが、Yocto buildとdependency-free MCP runtimeの必須toolではないためbuild-host gateから除外した。再実行はhost tools、Python、RAM、locale、AGL source、build conf、cache free-spaceをすべてPASSした。BitBakeは意図的にenvironmentをsourceしていないため`UNKNOWN`であり、setup gateでは失敗条件にしない。

### Hypotheses retained

1. qemux86-64 artifactをmini PCでbuildしMac TCGで動かす経路は最短baselineだが、LLVM/llvmpipe crashを維持する可能性がある。
2. qemuarm64 artifactをmini PCでbuildしMac HVFで動かす経路はCPU実行を改善するが、guest-visible graphics device/Vulkan要件が不足する可能性がある。

## Final foundation safety review

### Facts

- evidence envelopeへsubject/revision identity、sufficiency、audit correlationを追加した。identity未設定の観測はbaseline根拠にできず、明示的にUNKNOWNとなる。
- remote AGL/Yocto/command MCPはrole aliasとfixed entry pointに限定し、remote project HEADが設定済みpushed revisionと一致しなければ起動しない。
- remote MCPは一致HEADに加えてtracked/staged/untracked candidateがcleanであることを要求し、改変runtimeや追加runbookを読み込まない。
- 初期command catalogは短いread-only observation 2件だけに縮小した。parse、dry-run、effective environment、image buildは、persistent process ownership、preflight、lossless variable capture、artifact inventoryが未実装のため公開していない。
- Yocto task logは`tmp/work/.../temp/log.do_*`をcontext限定で発見・tailできるようにした。large tree scanは4,000 files/32 MiBで停止するため、metadata/task-logをrole-local subrootへ分割し、`truncated`をabsence evidenceにしない。
- directory/fileのpermission・I/O omissionも`truncated=true`へ伝播し、読めないtreeからempty resultを完全なnegative evidenceとして返さない。
- MCP input schemaをruntimeでもfail-closedに検証し、未定義fieldを無視しない。response cap超過時も再query用path/cursor/line locatorをbounded summaryへ保持する。
- run record/completion auditへplan digest、root role、timeout、exit、output digest/truncationを追加した。server異常終了でcompletion eventが欠ける場合の検出はlong-running acceptanceへ残す。
- privacy gateをtracked/untracked candidate全体へ適用し、email、private key marker、high-confidence token、IPv4/full/compressed IPv6、private hostname、個人home pathを検査する。checkerは一致値を表示しない。
- Git外`.fluorite-mcp` role configはMCPのwalk/direct read/root指定から拒否し、built-in `repository` aliasの上書きも拒否する。
- 2 MiBを超えるtask logは末尾最大256 KiBだけをstreaming readし、line identityをUNKNOWN、`truncated=true`として返す。catalog capはrequested suffix/name filter後に適用する。
- captured Raspberry Pi `bblayers.conf`のworkspace layer依存をmaterializer preflightへ追加した。writeにはsource identityを外部検証したという明示assertionが必要で、fresh pathはofficial `aglsetup.sh`を優先する。
- materializerは2つのconfを一時directoryで完了後にdirectory単位でpublishし、途中失敗で片方だけを残さない。
- GitHub Actionsはimmutable commit SHAへ固定した。
- `make verify`は61 unit tests、46 MCP focused tests、107 internal links、16 shell files、171 Git candidate filesを検査してPASSした。
- `make setup`、Mac foundation、qemux86-64、qemuarm64のcheck-only setupはPASSした。QEMU guest/imageは起動していない。
- pre-push Git metadata gateで未pushfeature checkpointの非role author/committerを検出した。値を表示せず、feature区間だけを汎用integration roleへ書き換え、既存remote `main`履歴は変更しない。
- staged差分をproject-authored fileとbyte-locked baseline artifactへ層別した。前者の`git diff --cached --check`はPASSし、後者の取り込み元由来の行末空白はhash対応を壊さないため保持した。後者はfile count/hash testで完全性を検査する。
- final checkerの追加指摘で、local/global Git excludeされたrunbook JSONがproduction globへ入るcommit境界の不足を検出した。production catalogをcode上の固定filenameへ限定し、追加JSONを拒否した。remote launcherはignored runtime surfaceも拒否し、Python環境path/site customizationを無効化する。任意command、実行gate、runbook effect classは変更していない。
- staged candidate専用gateを追加した。非ignoreのuntracked候補を拒否し、authored fileのcached snapshotと未stage差分をwhitespace検査する。byte-locked baselineの限定pathはhash/file-count testへ委譲し、例外理由と変更時の再検査条件を文書化した。
- independent checkerはworking-tree blockerなし、PASS WITH CONDITIONSと判定した。conditionsはcommit/push/clean clone、remote handshake、fresh agent、BitBake/QEMU/実機acceptance。

### Inferences

repository candidateは、安全な観測・役割分離・Mac QEMU前提確認までをfresh cloneへ渡せる構造になった。これは3D scene描画やYocto build成功を意味しない。

### UNKNOWN

- pushed revisionをLinux build roleへcheckoutした後のPython 3.10 remote MCP handshake。
- clean cloneでの共通gate、GitHub CI、branch policy、fresh Codex taskのcustom-agent routing。commit/push後に確認する。
- actual AGL source/build treeをsubroot化したときのMCP coverageとidentity assertion。
