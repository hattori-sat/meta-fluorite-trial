# Copy to .fluorite-mcp/remote-role.conf on the Codex/QEMU host.
# Use an SSH config alias; keep the hostname and account in the user's SSH config.
ssh_alias=build-role
remote_repository=/role/path/to/canonical/repository
# Replace this marker with the full commit pushed and checked out on both roles.
expected_project_revision=REPLACE_WITH_40_HEX_COMMIT
# This is an additional gate. The remote JSON config and MCP approval must also allow execution.
allow_command_execution=0
